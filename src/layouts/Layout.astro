---
import '../styles/global.css';

import logo from '../assets/todds-bistro-small-logo.png';
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="A recipe site for the curious and advertisement averse">
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>Todd's Bistro</title>
    </head>
    <body>
        <div class="mx-auto px-2">
            <div class="mx-auto mt-2 border-1 rounded-md p-2 md:w-1/2 w-full">
                <div class="flex flex-col items-center w-full mx-auto border-4 rounded-md p-2">
                    <header class="w-full">
                    <img src={logo.src} alt="Todd's Bistro Logo" class="w-full rounded-md" />
                    </header>
                    <menu class="w-full mx-2 mt-2 flex items-center justify-center border-t-2 border-b-2 
                        bg-champagne md:text-2xl font-bold">
                        <div>Categories</div>

                    </menu>
                    <slot />
                </div>
            </div>
        </div>
    </body>
</html>